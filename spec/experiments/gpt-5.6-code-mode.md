# GPT-5.6 Code Mode

Evaluate opt-in Codex Code Mode in Pi, exposing only freeform `exec` and `wait` while composing normal tools locally.

- Verify model/provider compatibility, history resume, permissions, rendering, and unified-exec behavior.
- Test validated global and project-local custom tools.
- Compare prompt/schema cost and reliability with normal structured tools.

Reference: `../references/workflow/igorwarzocha--howaboua-pi-stuff/packages/pi-codex-conversion`.
